BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[CK_Ligne] ALTER COLUMN [Oui] NVARCHAR(1000) NOT NULL;
ALTER TABLE [dbo].[CK_Ligne] ALTER COLUMN [Non] NVARCHAR(1000) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
